apiVersion: v1 
kind: Pod
metadata:
  labels:
    app: echo_server
  name: pod-example1
  namespace: default
spec:
  containers:
    - image: yuhaohu/cloud-os
      name: nettest1
      ports:
        - containerPort: 80