apiVersion: v1 
kind: Pod
metadata:
  labels:
    app: echo_server
  name: pod-example2
  namespace: default
spec:
  containers:
    - image: nginx-version-two
      name: nettest2
      ports:
        - containerPort: 80