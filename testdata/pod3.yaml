apiVersion: v1 
kind: Pod
metadata:
  labels:
    app: simple_service
  name: pod-example3
  namespace: default
spec:
  containers:
    - image: yuhaohu/cloud-os
      name: nettest1
      ports:
        - containerPort: 81